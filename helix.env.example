# Helix
# Copy to ~/.config/helix.env and set real values.
# All configuration uses HELIX_* env vars.

# OpenClaw gateway HTTP proxy (only needed if you use /api/gateway/* endpoints)
GATEWAY_HTTP_HOST=localhost
GATEWAY_HTTP_PORT=18789
GATEWAY_AUTH=

# Custom WebSocket URL for gateway (auto-derived from GATEWAY_HTTP_HOST/PORT if unset)
# GATEWAY_WS_URL=ws://localhost:18789/ws

# Legacy media-upload service proxy (disabled by default)
# ENABLE_MEDIA_UPLOAD_PROXY=1
# MEDIA_UPLOAD_HOST=localhost
# MEDIA_UPLOAD_PORT=18796

# Enable CORS for local development only (never in production)
# HELIX_DEV_CORS=1

# Whisper transcription settings
# HELIX_WHISPER_MODEL=base
# HELIX_WHISPER_DEVICE=cpu
# HELIX_WHISPER_TIMEOUT_MS=120000

# Agent workspace mapping (JSON): maps agent IDs to filesystem paths for introspection
# HELIX_AGENT_WORKSPACES={"main":"/path/to/main-agent","assistant":"/path/to/assistant"}

# Skill directories (colon-separated): where to look for SKILL.md files
# HELIX_SKILLS_DIRS=/usr/lib/node_modules/openclaw/skills:/home/user/skills

# Additional upload directory allowed for Whisper transcription
# HELIX_UPLOAD_DIR=/path/to/uploads
